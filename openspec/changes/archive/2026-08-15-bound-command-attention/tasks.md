## 1. Public Behavior Contract

- [x] 1.1 Extend the independently runnable command-attention and commit-checking Behave roots for default bounds, environment/CLI precedence, local result limits, and the exact deep proposed-file effect workflow.
- [x] 1.2 Run both affected feature roots fail-first and record the missing attention options, metadata, and deep-file boundary behavior.

## 2. Attention Utilities

- [x] 2.1 Add unit case matrices for positive/unlimited validation, environment resolution, CLI precedence, annotation-only counting, and envelope truncation composition.
- [x] 2.2 Implement the typed Python attention policy and canonical parser-only annotation inspection without importing expanders or invoking hooks.
- [x] 2.3 Extend host request/response handling for parser-only inspection and explicit effective attention metadata.

## 3. Public Command Wiring

- [x] 3.1 Add global `--commit-limit` and `--node-limit`, preserve command-local result limits, and pass effective policy on every repository service request.
- [x] 3.2 Compose native attention usage into JSON/human query and check results, including stable incomplete-history validation behavior.
- [x] 3.3 Update README and `zmem-author-commits` so `check --file ... --deep` is the primary effect-validation workflow and unlimited traversal is explicit.

## 4. Verification and Reconciliation

- [x] 4.1 Run focused utility, host, CLI, and both affected Behave roots GREEN.
- [x] 4.2 Run lock, supported-interpreter, lint, format, complete unit and independently runnable Behave roots, skill validation, and clean package-build gates.
- [x] 4.3 Reconcile mature behavior into canonical command-attention, memory-cli, and commit-checking specs and validate OpenSpec strictly.
