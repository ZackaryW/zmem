## 1. Shape Public Behavior

- [x] 1.1 Add service-management BDD scenarios for exact-version remote installation and corrupt-artifact rejection through the CLI
- [x] 1.2 Add focused unit matrices for platform target normalization, strict manifest parsing, integrity enforcement, and local-source precedence
- [x] 1.3 Run the focused tests and record the expected RED before implementation

## 2. Release Resolution Utilities

- [x] 2.1 Implement strict release manifest and asset value objects under `zmem/utils`
- [x] 2.2 Implement operating-system and machine alias normalization to supported Rust targets
- [x] 2.3 Implement bounded standard-library manifest retrieval and streaming asset verification beneath runtime staging
- [x] 2.4 Run focused unit tests GREEN independently

## 3. Service Wiring

- [x] 3.1 Preserve strict local binary precedence and fall back to exact-version GitHub release acquisition only when local discovery is absent
- [x] 3.2 Feed verified downloads through existing identity, compatibility, activation, health, and rollback behavior
- [x] 3.3 Document `uv tool`/`uvx` bootstrap behavior, release source override, and offline local-source alternatives
- [x] 3.4 Run the service-management capability BDD root independently

## 4. Verification

- [x] 4.1 Check the uv lock and supported Python interpreter
- [x] 4.2 Run Ruff format/check, the complete unit suite, and every capability-owned BDD root independently
- [x] 4.3 Build the sdist and wheel through `uv_build` and inspect that no native artifact is accidentally bundled
- [x] 4.4 Strictly validate the OpenSpec change
