## 1. Regression Authority

- [ ] 1.1 Add one independently runnable memory CLI scenario proving a default empty search omits `trail` while `--trail` restores the unchanged identity object.
- [ ] 1.2 Add thin established step bindings that exercise the real CLI through shared lifecycle support and demonstrate RED before implementation.

## 2. Public CLI Correction

- [ ] 2.1 Add the search-local `--trail` parser option and conditionally pass the already-decoded trail to the public envelope.
- [ ] 2.2 Prove the focused scenario GREEN and run the complete memory CLI Behave feature.

## 3. Consumer Guidance

- [ ] 3.1 Update README search examples and selected-trail guidance for the default omission and explicit opt-in.
- [ ] 3.2 Update the repository memory-query skill contract so provenance workflows request `search --trail` when selected-trail identity is required.

## 4. Verification

- [ ] 4.1 Run focused Python tests for CLI/output behavior plus the complete Python test suite.
- [ ] 4.2 Run repository lint, format checks, OpenSpec validation, and the affected Behave suite through the established runners.
